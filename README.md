# DevAssist

### An AI assistant built with Groq API and made for developers.

## Commands
- Explain (explain what a file does)
- Refactor (rewrite a file more efficiently)
- Generate (generate a function (Only python support ATM))
- Debug (edits the file and debugs)
- Chat (interactive chat mode, commands are usable)